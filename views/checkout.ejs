<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon/fonts/remixicon.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto my-8 p-4 bg-white shadow-md rounded-lg">
        <h2 class="text-2xl font-bold mb-4">Checkout</h2>
        <div class="flex justify-between items-center mb-4">
            <div>
                <h4 class="text-lg font-semibold"><%= product.name %></h4>
                <p class="text-gray-600"><%= product.description %></p>
            </div>
            <div>
                <p class="text-lg font-bold text-green-600">$ <%= product.price %></p>
            </div>
            <div class="flex ">
                <a href="/cart/addmoretocart/<%= product._id %>" class="bg-blue-600 text-white p-2 rounded-lg"><i class="ri-add-line"></i></a>
                <% const cartItemCount = user.cart.filter(item => item._id.toString() === product._id.toString()).length; %>
            <% if (cartItemCount > 0) { %>
                <p class="text-lg font-bold text-green-600 mx-2 p-2">x<%= cartItemCount %></p>
            <%}%>
            <a href="/cart/removefromcart/<%= product._id %>" class="bg-red-600 text-white p-2 rounded-lg"><i class="ri-subtract-line"></i></a>
            </div>
        </div>
        <form action="/checkout" method="POST" class="mt-4">
            <input type="hidden" name="totalPrice" value="<%= product.price %>">
            <input type="hidden" name="productId" value="<%= product._id %>">
            <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded">
                Confirm Purchase
            </button>
        </form>
    </div>
</body>
</html>